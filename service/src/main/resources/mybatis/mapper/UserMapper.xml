<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dygstudio.epsms.service.mapper.UserMapper">
    <select id="findUserByNameAndPassword" resultMap="UserResultMap" parameterType="String">
        select * FROM User WHERE name=#{name} and password=#{password}
    </select>
    <select id="findUserById" resultMap="UserResultMap" parameterType="String">
        select * FROM User WHERE id=#{userId}
    </select>
    <select id="findUserByName" resultMap="UserResultMap" parameterType="String">
        select * FROM User WHERE name=#{userId}
    </select>
    <select id="findAllUser" resultMap="UserResultMap" parameterType="Map">
        select * FROM User limit #{pageStart},#{pageSize}
    </select>
    <select id="countUser" resultType="String">
        select count(0) from User
    </select>
    <select id="findUserByObject" resultMap="UserResultMap" parameterType="Map">
        select * FROM User
        <where>
            <if test="User.id!=null and User.id.trim()!=''">
                and id = #{User.id}
            </if>
            <if test="User.name!=null and User.name.trim()!=''">
                and name = #{User.name}
            </if>
            limit #{pageStart},#{pageSize}
        </where>
    </select>
    <update id="update" parameterType="User">
        update User
        <trim prefix="set" suffixOverrides=",">
            <if test="name!=null">name = #{name},</if>
            <if test="password!=null">password = #{password},</if>
        </trim>
        WHERE
        (id = #{id});
    </update>
    <insert id="insert" parameterType="User">
        insert into User
        (
            id,name,password
        )
        values
        (
            #{id},
            #{name},
            #{password}
        )
    </insert>
    <!-- MySQL下批量保存，可以foreach遍历 mysql支持values(),(),()语法 -->
    <insert id="insertList" parameterType="User">
         insert into User
        (
            id,name,password
        )
        values
        <foreach collection="userList" item="user" separator=",">
            (
            REPLACE(UUID(),"-",""),
            #{user.name},
            #{user.password}
            )
        </foreach>
    </insert>
    <delete id="deleteById" parameterType="String">
        delete from User WHERE id=#{userId}
    </delete>
    <resultMap id="UserResultMap" type="User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
    </resultMap>
</mapper>